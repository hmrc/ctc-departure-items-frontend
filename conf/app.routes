# microservice specific routes

->          /                                            app.item.Routes

->          /items/hmrc-frontend                         hmrcfrontend.Routes
GET         /:lrn/items/assets/*file                     controllers.AssetsController.versioned(path="/public", file: Asset, lrn: LocalReferenceNumber)

GET         /:lrn/items                                  controllers.RedirectController.redirect(lrn: LocalReferenceNumber)
